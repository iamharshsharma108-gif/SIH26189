# Optional Google/OIDC + SMTP configuration for CYBER RAKSHAK FINAL.
# Never commit real secrets to source control.

[auth]
# Local development example. Change this if your app runs elsewhere.
redirect_uri = "http://localhost:8501/oauth2callback"
cookie_secret = "REPLACE_WITH_A_LONG_RANDOM_SECRET"
client_id = "YOUR_GOOGLE_OAUTH_CLIENT_ID"
client_secret = "YOUR_GOOGLE_OAUTH_CLIENT_SECRET"
server_metadata_url = "https://accounts.google.com/.well-known/openid-configuration"

[smtp]
host = "smtp.example.com"
port = 587
username = "your-mailbox"
password = "YOUR_SMTP_SECRET"
from_email = "your-mailbox@example.com"
starttls = "true"


# EMAIL DELIVERY SERVICE
# Example for a mailbox provider with SMTP submission. Keep the real password/app secret private.
[smtp]
host = "smtp.example.com"
port = 587
username = "your-mailbox@example.com"
password = "YOUR_SECRET"
from_email = "your-mailbox@example.com"
starttls = "true"
