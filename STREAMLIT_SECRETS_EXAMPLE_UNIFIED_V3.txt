# Copy this structure into .streamlit/secrets.toml and replace placeholders.
# Never commit real SMTP credentials to source control.

[smtp]
host = "smtp.example.com"
port = 587
username = "your-mailbox"
password = "YOUR_SECRET"
from_email = "your-mailbox@example.com"
starttls = "true"
